version: '3.4'

services:
  simplecliniqapi:
    image: ${DOCKER_REGISTRY-}simplecliniqapi
    build:
      context: .
      dockerfile: src/SimpleCliniq.Api/Dockerfile
  simple.fe:
     image: chbudi9/simplecliniqfe:latest
     container_name: simplecliniq.fe
     restart: always
     ports:
       - 8090:8080
